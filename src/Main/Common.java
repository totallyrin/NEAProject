package Main;

public enum Common {
}

enum Direction {
    UP, DOWN, LEFT, RIGHT, NULL
} // marks for left, right, up and down

enum Mark {
    WALL, PATH, CURRENT, END
} // marks for wall, path or current cell

